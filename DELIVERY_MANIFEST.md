# RDK S100 四功能融合巡检系统交付清单

交付版本：`1.0.0`  
交付日期：`2026-08-06`  
目标硬件：`RDK S100 + GS130W 双目相机`

## 本次交付内容

- 单路 ROS2 相机订阅、容量为 1 的最新帧缓冲和单次 HBM 推理。
- 同一帧结果同时分发给车辆违停、夜间人员/人群、明火三个本地规则引擎；垃圾桶在点位停留窗口内选择最佳完整画面，进入 MiniMax 异步持久复核队列。
- AprilTag `tag36h11` 优先定位，识别失败时回退旧视觉指纹；两者均失败时不加载敏感区域。
- GS130W 双目标定、明火测距和距离不可用降级路径。
- 固定报警字段、中文关键帧、只读局域网页、离线报警台账和报警拉回脚本。
- 原始双目组合视频及标注主视图视频分段保存，默认留存 72 小时。
- MapPilot 首版边界：仅预留到点、暂停、继续和离点接口，不包含运动控制。
- RDK systemd 部署、升级持久配置、预检、标定、点位登记及现场验收工具和文档。
- GS130W 固定 `1m → 3m → 5m` 网页测距验证助手：每点自动采集3个点击后的新同步帧、共用一次ROI、严格质量门槛、失败证据留存、断电可重放事务和完整审计包拉回。
- 单独的生产安装审核器：锁定候选、验证器和三份正式双目运行时源码 SHA256，并从9对校正图独立重算SGBM、距离及质量指标；验证工具本身不会覆盖生产标定。

## 验证结果

- 全量自动化测试：`65 passed`
- Python 3.9 语法兼容检查：通过
- Bash 部署脚本语法：通过
- PowerShell 部署脚本语法：通过
- `validate-config`：通过
- `preflight`：`0 error / 4 warning`
- 离线报警台账导出：通过
- 未发现缓存文件、生产密钥或旧工程绝对路径
- 三点验证器 Python 3.9 编译、自测、7个最终化断电故障点及两种审计短尾边界：通过
- 1m/3m/5m 合成运行时视差测试：通过
- Windows 三点启动器 PowerShell 5.1 Parser、DryRun、坏ZIP转SCP和完成会话只读拉回夹具：通过
- 安装审核器完整正向会话与黑图伪造拒绝测试：通过（最终打包前复核）

HBM 模型：

`models/hbm/patrol_v2_yolo11s_static_640x320_rect_rgb_int8.hbm`

SHA256：

`bdd41c37a92dbbc73fb6c9ba87db81994bbcfdf9b93e1e4aee856b23376b5612`

完整文件哈希见 `SHA256SUMS.txt`。

## 生产启用前必须完成

1. 在实际 GS130W 上确认物理左右目并完成双目标定。
2. 使用至少三个已知距离点验证明火距离。
3. 打印、安装并登记 AprilTag；在物理左目主视图重画车辆 ROI，并为车辆和垃圾点位采集视觉指纹。
4. 在板端环境文件中配置真实 `MINIMAX_API_KEY`。
5. 在 RDK S100 上完成至少 15 FPS、24 小时稳定运行和 72 小时录像留存验收。

这些项目依赖现场相机、点位、密钥、网络和存储，开发机测试不能替代。
